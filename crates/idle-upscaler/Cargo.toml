[package]
authors = ["idlescreen developers"]
repository = "https://github.com/idlescreen/idlescreen"
name = "idle-upscaler"
version = "0.5.0"
edition = "2024"
description = "CPU upscaling for idle screensaver frames"
license = "Apache-2.0"

[lints]
workspace = true

[dependencies]
idle-api = { path = "../../idle-api" }
idle-log.workspace = true